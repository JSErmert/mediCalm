Resume from last state.

Milestone: M6.8.3.2 Fix — Continue What Helped must use the canonical guided breathing UI

This is a targeted correction.
Do NOT redesign architecture.
Do NOT explore the repo.
Do NOT create a new breathing screen.
Do NOT use any detached or alternate runtime UI.

----------------------------------------
PROBLEM TO FIX

"Continue What Helped" is currently routing to the wrong guided session UI.

It must NOT route to a separate or simplified session screen.
It must route to the SAME existing guided breathing screen / orb-based runtime used by the normal session flow.

The desired behavior is:

- user taps "Continue What Helped"
- system builds/stores the memory-informed prescription
- system navigates to the canonical orb-guided breathing screen
- that existing screen reads the stored prescription and runs it
- the user sees the same trusted breathwork UI as normal sessions

In other words:
Continue What Helped = smarter entry into the EXISTING guided session screen
NOT a separate breathing experience

----------------------------------------
UI COPY FIX

On the home screen, inside the same card/box:

Title:
Continue What Helped

Subtext:
Based on what helped recently

The subtext must appear inside the same box/card under the title.
Keep it calm and minimal.

----------------------------------------
IMPLEMENTATION RULES

1. Reuse the canonical guided breathing screen only
2. Do NOT route to M6GuidedSessionScreen or any detached runtime UI
3. Do NOT create a new screen
4. Preserve existing orb behavior, layout, timing feel, and controls
5. Keep the prescription handoff through app state if that is already how it works
6. The canonical guided screen should read pendingBreathPrescription first if present, otherwise fall back to normal session config logic

----------------------------------------
LIKELY FILES TO MODIFY

Only modify what is needed, likely including:
- src/screens/HomeScreen.tsx
- src/screens/GuidedSessionScreen.tsx
- src/context/AppContext.tsx
- src/context/AppProvider.tsx
- src/App.tsx

Do NOT modify unrelated engine logic unless absolutely necessary.

----------------------------------------
RETURN ONLY:
1. brief confirmation
2. changed files
3. full contents of each changed file

No explanations.
No questions.