Resume from last state.

Milestone: M6.5 — Session Configuration Layer

You already have full context. Do NOT ask questions.

Before implementing:
1. Give brief, honest feedback on whether M6.5 is the correct next milestone after M6.3 and M6.4.
2. If it is NOT the right next step, say so clearly and stop.
3. If it IS the right next step, proceed immediately with implementation.

Memory / efficiency rules:
- Do NOT explore the repo
- Do NOT re-read files unless absolutely required
- Do NOT restate the project
- Do NOT brainstorm alternatives
- Do NOT think step-by-step
- Execute directly from the defined architecture

Current architecture:
- M6.3 intake UI is complete and stores state interpretation result
- M6.4 interpretStates(...) is complete and exported
- M6.5 should convert StateInterpretationResult into a deterministic session blueprint for the guided experience layer

Scope:
- Create/complete ONLY:
  src/engine/hari/sessionConfig.ts
  src/types/hari.ts (append M6.5 types only if needed)
  src/engine/hari/index.ts (export only if needed)

Do NOT:
- Modify M6.3 intake UI or screens
- Modify M6.4 interpretation logic
- Add adaptive learning
- Redesign UX
- Add extra features outside M6.5

Goal:
Build a pure deterministic function that converts StateInterpretationResult into SessionConfig.

Function target:
- buildSessionConfig(result: StateInterpretationResult): SessionConfig

SessionConfig should include only the core execution blueprint needed by the next layer:
- sessionName
- breathPattern
- inhaleSeconds
- holdSeconds
- exhaleSeconds
- effortLevel
- instructionTone
- openingPrompt
- durationSeconds
- bias
- overloadSafe

Required logic:
1. Preserve the breathPattern and effortLevel determined by M6.4 unless explicit session shaping requires a direct timing mapping
2. Map breathPattern into concrete timing:
   - 2/4 -> inhale 2, hold 0, exhale 4
   - 3/5 -> inhale 3, hold 0, exhale 5
   - 4/7 -> inhale 4, hold 0, exhale 7
3. Use overload-safe configuration when overload = true:
   - short duration
   - minimal language
   - soft/simplifying tone
   - opening prompt should reduce pressure and cognitive load
4. Map bias to instruction tone and session naming:
   - protect_decompress -> protective / gentle
   - restore_minimize_effort -> restorative / minimal
   - simplify_downshift -> simplifying / quiet
   - calm_downregulate -> calming / direct
   - support_stabilize -> supportive / steady
   - release_expand -> expansion-focused / spacious
   - release_ground -> grounding / release-oriented
5. Keep prompts short, human, and low-load
6. Duration should be deterministic and simple:
   - minimal effort -> shorter
   - reduced effort -> medium-short
   - standard effort -> medium
7. No side effects
8. No UI dependencies
9. No AppContext usage

Implementation guidance:
- This is a bridge layer from interpretation -> session execution blueprint
- Keep it small, clean, deterministic, and implementation-ready
- Do not overbuild

Return ONLY:
1. Brief honest feedback (2-5 lines max)
2. Full contents of src/engine/hari/sessionConfig.ts
3. Any appended M6.5 types in src/types/hari.ts
4. Any index.ts export change if required

No explanations beyond the brief feedback.
No questions.