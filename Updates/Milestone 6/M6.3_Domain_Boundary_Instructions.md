You are implementing M6.3_Dynamic_Intake_Screen for mediCalm.

Your domain is strictly the intake UI/input layer.

## Your responsibility
Build the dynamic intake experience that renders the correct fields based on selected M6 state(s), collects the required inputs, and passes a clean payload forward.

## You may modify
- intake screen component files
- intake-specific UI helpers
- intake validation helpers
- intake-related presentation logic
- local intake submission logic

## You may read but should avoid modifying
- shared type files
- AppContext
- navigation wiring
- shared constants

Only modify these if absolutely necessary to complete M6.3, and keep any such change minimal.

## You must not modify
- HARI engine / interpretation logic
- state classification logic
- conflict resolution hierarchy
- session framing computation
- breath pattern selection logic
- overload protocol logic
- session output generation
- guided session layer behavior

Those belong to M6.4.

## Frozen contract to build against

Assume the intake layer sends this payload shape forward:

- `state_entry: EntryState[]`
- `intensity: number`
- `sensitivity?: 'low' | 'moderate' | 'high'`
- `location?: string | string[] | null`

You may adapt the exact location representation only if the repo already has a preferred pattern, but do not redesign the overall contract.

## Field rules
- Pain → location, intensity, sensitivity
- Tight → optional location, intensity
- Anxious → intensity only
- Angry → intensity only
- Sad → intensity only
- Exhausted → intensity only
- Overwhelmed → intensity only

## Multi-select rules
- Show location only if Pain or Tight is selected
- Show sensitivity only if Pain is selected
- Use one unified intensity input

## Critical constraints
- Do not invent new state logic
- Do not create multiple intensities
- Do not interpret the user emotionally
- Do not redesign downstream HARI output
- Do not expand scope into session logic

## Implementation style
- Keep cognitive load low
- Keep UI simple and fast
- Collect only what improves the session
- Preserve mediCalm’s physiological framing

## Goal
Finish M6.3 without stepping into M6.4’s engine domain.