Resume from last state.

Milestone: M6.8.4 — Runtime Unification + M7 Boundary Cleanup

This is a targeted cleanup milestone before M7.1.
Do NOT begin M7.1 yet.
Do NOT redesign architecture.
Do NOT explore the repo.
Do NOT re-check older milestones.

Goal:
Unify all guided session runtime behavior onto the canonical orb-based GuidedSessionScreen, restore clean M6/M7 layer boundaries, and add the minimal hook point needed for future M7.1 timed guidance.

----------------------------------------
PART 1 — Runtime Unification

Fix the duplicate session screen problem.

Requirements:
- Retire M6GuidedSessionScreen from the active runtime path
- Route all M6-originated sessions and Continue What Helped sessions through the canonical GuidedSessionScreen
- Do NOT create any new session screen
- Preserve existing orb-based UI, timing feel, controls, and progress behavior
- Keep pendingBreathPrescription handoff working

Result:
There must be exactly one authoritative guided session runtime screen.

----------------------------------------
PART 2 — Single Runtime Clock

Ensure GuidedSessionScreen is the sole runtime authority.

Requirements:
- progress/fill bar, remaining time, and completion must all use the same session clock
- when progress reaches 100%, the session ends immediately
- remove or bypass any leftover round-count / breath-count / alternate timer completion logic for M6 sessions
- no duplicate timer logic from M6GuidedSessionScreen may remain in active use

----------------------------------------
PART 3 — M6 / M7 Boundary Cleanup

Fix layer leakage.

Requirements:
- M6 must produce the raw BreathPrescription only
- M7 adaptation / delivery shaping must NOT be embedded inside buildSessionConfig
- move applyHARIAdaptation (or equivalent adaptation logic) out of buildSessionConfig into a later delivery-boundary wrapper or equivalent staging point
- preserve current functionality as much as possible while restoring the boundary

Goal:
M6 = what session to run
M7 = how to deliver it

----------------------------------------
PART 4 — Cue Hook Preparation

Prepare GuidedSessionScreen for future M7.1 timed guidance without implementing full guidance yet.

Requirements:
- add a minimal cue insertion point to GuidedSessionScreen
- introduce a currentCue state (or equivalent)
- add a clean render slot for future cue text
- wire the hook so future M7.1 can drive cues from elapsed time and/or round completion
- do NOT implement full cue scheduling yet
- do NOT add multiple cues or dense messaging

Goal:
M7.1 should later be able to plug in cleanly without restructuring the screen again.

----------------------------------------
PART 5 — Continue What Helped Preservation

Requirements:
- Continue What Helped must continue to route into the canonical GuidedSessionScreen
- it must still use pendingBreathPrescription if present
- it must not route into any detached or retired runtime screen

----------------------------------------
CONSTRAINTS

- Do NOT modify unrelated engine logic
- Do NOT reintroduce breath counts or round counters
- Do NOT add charts, analytics, or new UI surfaces
- Do NOT implement full M7.1 guidance yet
- Keep the system calm, minimal, and canonical

----------------------------------------
LIKELY FILES TO MODIFY

Only modify what is needed, likely including:
- src/screens/GuidedSessionScreen.tsx
- src/screens/M6GuidedSessionScreen.tsx
- src/App.tsx
- src/context/AppContext.tsx
- src/context/AppProvider.tsx
- src/engine/hari/sessionConfig.ts
- src/engine/hari/index.ts
- any adaptation wrapper file if needed

----------------------------------------
RETURN ONLY:
1. brief confirmation
2. changed files
3. full contents of each changed file

No explanations.
No questions.