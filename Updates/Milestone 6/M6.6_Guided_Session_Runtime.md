Resume from last state.

Milestone: M6.6 — Guided Session Runtime

Context shift:
Previous work in this thread completed M6.4 and M6.5 engine-layer logic.
This task is now M6.6, which is a UI/runtime integration milestone.

You already have full context. Do NOT ask questions.

Before implementing:
1. Give brief, honest feedback on whether M6.6 is the correct next milestone after M6.3, M6.4, and M6.5.
2. If it is NOT the right next step, say so clearly and stop.
3. If it IS the right next step, proceed immediately with implementation.

Memory / efficiency rules:
- Do NOT explore the repo
- Do NOT re-read files unless absolutely required
- Do NOT restate the project
- Do NOT brainstorm alternatives
- Do NOT think step-by-step
- Execute directly from the defined architecture

Current architecture:
- M6.3 intake UI is complete
- M6.4 interpretStates(...) is complete and exported
- M6.5 buildSessionConfig(...) is complete and exported
- stateInterpretationResult is already stored in app state
- M6.6 should run the actual guided session experience from SessionConfig

Scope:
- Modify ONLY the minimum files required to create the guided session runtime
- Reuse existing architecture and routing wherever possible
- If a new screen is required, create only what is necessary
- Read from existing app/context state rather than redesigning state flow

Do NOT:
- Modify M6.4 interpretation logic
- Modify M6.5 sessionConfig logic unless absolutely required for integration
- Redesign intake UX
- Add adaptive learning
- Add analytics/history systems
- Add extra features outside M6.6

Goal:
Create the first deterministic guided session runtime that takes the existing interpretation/config pipeline and turns it into a live breathing session.

Required behavior:
1. Guided session screen must display:
   - sessionName
   - openingPrompt
   - visible current breath phase
   - remaining time
2. Guided session must use SessionConfig timing:
   - inhaleSeconds
   - holdSeconds
   - exhaleSeconds
   - durationSeconds
3. Guided session must support:
   - start
   - pause/resume
   - end session early
4. Guided session must progress through breath phases deterministically
5. Guided session must complete cleanly when duration reaches zero
6. Completion state should be simple and low-load
7. Overload-safe sessions should remain visually and cognitively minimal

Implementation guidance:
- Keep the UI simple
- Use existing screen/navigation patterns
- Prefer minimal text and low cognitive load
- Do not over-animate
- Do not overbuild abstractions
- This milestone is about making the full pipeline runnable end-to-end

Preferred architecture:
- Use the existing state/stateInterpretationResult flow
- Build SessionConfig from stored interpretation result
- Route into a guided runtime screen
- Keep runtime logic localized and clean

Return ONLY:
1. Brief honest feedback (2–5 lines max)
2. The changed files
3. Full contents of each changed file

No explanations beyond the brief feedback.
No questions.