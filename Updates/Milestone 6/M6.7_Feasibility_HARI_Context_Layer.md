Resume from last state.

Milestone: M6.7 — Feasibility + HARI Context Layer

You already have full context. Do NOT ask questions.

Before implementing:
1. Briefly confirm this is the correct next step after M6.6
2. If not, STOP
3. If yes, proceed

Memory rules:
- Do NOT explore repo
- Do NOT re-read unnecessary files
- Do NOT redesign architecture

Scope:

1. Create:
   src/engine/hari/feasibility.ts

   Implement:
   adjustForFeasibility(...)

   Apply rules:
   - overwhelmed → no counting, no holds, softer pacing
   - high sensitivity → reduce duration + complexity
   - mixed/uncertain → neutral breathing (3–3–6 or 4/6)
   - ensure inhale >= 3 where appropriate
   - ensure exhale > inhale

2. Create:
   src/engine/hari/context.ts

   Implement:
   basic HARIContext tracking:
   - last input
   - last result
   - last outcome
   - simple recency logic

3. Modify sessionConfig.ts:
   - incorporate FeasibleSessionProfile
   - REMOVE breath count logic entirely
   - enforce time-based sessions only

4. Do NOT:
   - modify UI
   - change M6.4 logic
   - add complexity beyond defined scope

Return ONLY:
- changed files
- full file contents