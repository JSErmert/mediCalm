Resume from last state.

Milestone: M6.8.2 — Finalize Runtime Integrity + HARI UI

M6.8 and M6.8.1 are implemented.
This is a targeted refinement milestone.
Do NOT re-implement architecture.
Do NOT explore the repo.
Do NOT begin M7.

Goal:
Finalize M6.8.2 so the runtime is trustworthy and HARI is visibly present in the UI.

----------------------------------------
PART 1 — Runtime Integrity

Fix the guided session runtime so there is exactly one authoritative session clock.

Requirements:
- The progress/fill bar, remaining time, and session completion must all use the same elapsed session clock.
- When the progress bar reaches 100%, the session must end immediately.
- Remove or bypass any remaining breath-count, round-count, or cycle-count completion logic for M6 sessions.
- Ensure orb, progress bar, remaining time, and completion trigger stay synchronized.

----------------------------------------
PART 2 — Naming Lock

Update UI naming so:
- Body Context -> Your State
- HARI Context / related visible memory UI -> Your Patterns

Use these names consistently.
Do not introduce alternate labels.

----------------------------------------
PART 3 — Your Patterns UI

Add or finalize a visible “Your Patterns” panel next to “Your State”.

Keep it minimal and calm.

Show only:
1. Last session
   - session name
   - approximate duration label
   - recency (Today / Yesterday / etc.)

2. What changed
   Use subtle shift language, not better/same/worse:
   - More relaxed
   - More open
   - More steady
   - More energized
   - No real change
   - More tense / tight

3. Simple memory text
   Examples:
   - “Short resets helped”
   - “Expansion felt better”
   - “We’ll keep this simple”

Constraints:
- No charts
- No analytics dashboard
- No raw data structures
- No percentages
- No performance framing

----------------------------------------
PART 4 — Feedback Refinement

Replace generic outcome handling with the subtle shift model.

If needed, update the relevant context/UI types so sessions can store and display:
- relaxed
- open
- steady
- energized
- no_change
- tense_tight

Use this for “Your Patterns”.
Do NOT implement M7 adaptation behavior yet.

----------------------------------------
Constraints

- Do NOT redesign orb UX
- Do NOT modify M6.4 or M6.8 architecture unless required for this refinement
- Keep the UI simple, calm, and low-load
- Preserve existing session behavior except where required to fix integrity

----------------------------------------
Return ONLY:
1. brief confirmation
2. changed files
3. full contents of each changed file

No explanations.
No questions.