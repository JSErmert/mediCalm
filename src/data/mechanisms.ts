/**
 * MediCalm Mechanism Registry — M2 placeholder.
 * Authority: Knowledge + Protocol Doctrine (doc 03)
 *
 * Populated in Milestone 2 when the deterministic session engine is built.
 * The type contract is already established in src/types/index.ts.
 */
import type { MechanismObject } from '../types'

export const MECHANISMS: MechanismObject[] = []
