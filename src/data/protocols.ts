/**
 * MediCalm Protocol Library — M2 placeholder.
 * Authority: Knowledge + Protocol Doctrine (doc 03), Protocol Library (doc 13)
 *
 * Populated in Milestone 2 when the deterministic session engine is built.
 * The type contract is already established in src/types/index.ts.
 */
import type { ProtocolDefinition } from '../types'

export const PROTOCOLS: ProtocolDefinition[] = []
